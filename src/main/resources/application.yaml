spring:
  application:
    name: campusstage

  datasource:
    url: jdbc:mysql://localhost:3306/campusstage?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: "mk010312"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080